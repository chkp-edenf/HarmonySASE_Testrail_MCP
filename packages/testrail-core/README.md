# testrail-core

TestRail integration core library. HTTP client, caches, rate limit, retry, schemas.

Pre-release skeleton. Populated by plan-004 phase 5.

When complete, importable as:

    from testrail_core import TestRailClient
