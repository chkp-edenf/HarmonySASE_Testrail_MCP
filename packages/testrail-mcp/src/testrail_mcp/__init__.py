"""testrail-mcp — MCP server wrapping testrail-core.

Skeleton in PR 5.0; the stdio entrypoint, dispatcher, gates, and tool
defs migrate from the legacy `src/` tree across PRs 5.1 through 5.11.
"""
__version__ = "0.1.0.dev0"
